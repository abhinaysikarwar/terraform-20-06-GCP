name: Deploy to Cloud Run

on:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: "projects/844840119181/locations/global/workloadIdentityPools/github-pool/providers/github-provider"
          service_account: "abhinay@project-ebf080ef-f8f0-47b3-806.iam.gserviceaccount.com"

      - name: Setup GCloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker
        run: |
          gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      - name: Build Image
        run: |
          docker build \
            -t us-central1-docker.pkg.dev/project-ebf080ef-f8f0-47b3-806/app-repo/demo-app:${{ github.sha }} \
            ./app

      - name: Push Image
        run: |
          docker push \
            us-central1-docker.pkg.dev/project-ebf080ef-f8f0-47b3-806/app-repo/demo-app:${{ github.sha }}

      - name: Deploy Cloud Run
        run: |
          gcloud run deploy demo-app \
            --image us-central1-docker.pkg.dev/project-ebf080ef-f8f0-47b3-806/app-repo/demo-app:${{ github.sha }} \
            --region us-central1 \
            --platform managed \
            --allow-unauthenticated
